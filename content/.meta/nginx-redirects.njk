---
permalink: ../nginx-redirects.txt
layout: null
---
# This file automagically generated by 11ty

# Redirect from v2 to v3 URLs (I know, but I don’t write often enough to make dated URLs worth doing)
rewrite ^/writing/\d+/\d+/(.*) /writing/$1 permanent;

# shortcode redirections
{%- for post in collections._posts %}
rewrite ^/r/{{ post.data.shortUrl }} {{ post.url }} redirect;
{%- endfor %}
